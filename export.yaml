# Paths are relative to the home directory and are exported below this repository's home/ directory.
# Add only files and directories that are safe to publish.
paths:
  - .codex/AGENTS.md
  - .codex/config.toml
  - .codex/browser/config.toml
  - .agents/ai-dotfiles/skill-links.json
  - .agents/scripts/check-lf.mjs

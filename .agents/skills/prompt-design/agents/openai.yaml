interface:
  display_name: "Prompt Design"
  short_description: "Design, review, and revise model-facing instructions"
  default_prompt: "Use $prompt-design to design, review, or revise this model-facing instruction for its target runtime, preserve intent and required procedures, and return a ready-to-use result with the checks performed and any remaining uncertainty when changes are requested."
